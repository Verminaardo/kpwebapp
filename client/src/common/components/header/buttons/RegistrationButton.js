import React from 'react';

const RegistrationButton = ({text}) => (
   <button type="button" className="btn btn-outline-danger mr-5">
      {text}
   </button>
);

export default RegistrationButton;
